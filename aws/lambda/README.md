### Lambda Functions use two values from SSM Parameter Store:

```
dragon_data_bucket_name (String): name of the S3 bucket
dragon_data_file_name (String): name of the file in the S3 bucket (JSON array of Dragon objects)
```
